
pipeline {
  agent any
  stages {
    
stage('Checkout') {
  steps {
    git url: 'https://github.com/basgabrielsPA/jenkins_test',
        branch: 'main',
        // credentialsId: 'github-creds'  // if private
}

    // … other stages …
  }
}
